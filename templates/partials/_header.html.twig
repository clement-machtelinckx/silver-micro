

<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
    <div class="container-fluid">

        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav me-auto">
                {% if app.user %}
                <li class="nav-item">
                    <a class="nav-link active text-white" href="{{ path('home') }}">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active text-white" href="{{ path('restaurants') }}">Restaurants</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active text-white" href="{{ path('user') }}">Profil</a>
                {% else %}
                <li class="nav-item">
                    <a class="nav-link active text-white" href="{{ path('login') }}">login</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active text-white" href="{{ path('register') }}">register</a>
                </li>
                {% endif %}

            </ul>
        </div>
        <div>
            {% if app.user %}
            <ul>
                <li class="nav-item">
                    <a class="nav-link active text-white" href="{{ path('logout') }}">logout</a>
                </li>
            {% endif %}
            </ul>
        </div>
    </div>
  </nav>